# Random Password Generator
import string
import random
len=int(input("Enter length of your password"))
lower = string.ascii_lowercase
upper = string.ascii_uppercase
num = string.digits
data=lower+upper+num
temp = random.sample(data,len)
password = "".join(temp)
print(password)
